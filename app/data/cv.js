/** CV / Resume link for navbar + about buttons (same-origin PDF). */
export const CV_DRIVE_URL = "/Franco-Juarez-CV.pdf";

/** Same-origin fallback PDF served from /public. */
export const CV_LOCAL_PATH = "/Franco-Juarez-CV.pdf";
